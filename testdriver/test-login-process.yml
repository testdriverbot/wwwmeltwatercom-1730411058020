version: 4.0.78
steps:
  - prompt: Click on "Log in" in the header.
    commands:
      - command: hover-text
        text: Log in
        description: link in the header
        action: click
  - prompt: Click on "Log in" in the header.
    commands:
      - command: hover-text
        text: Log in
        description: link in the header
        action: click
  - prompt: Click on "Log in" in the header.
    commands:
      - command: hover-text
        text: Log in
        description: link in the header
        action: click
