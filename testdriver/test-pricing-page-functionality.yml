version: 4.0.78
steps:
  - prompt: Click on "Pricing" in the header.
    commands:
      - command: hover-text
        text: Pricing
        description: link in the header
        action: click
  - prompt: Click on "Pricing" in the header.
    commands:
      - command: hover-text
        text: Pricing
        description: link in the header
        action: click
      - command: wait
        timeout: 3000
      - command: assert
        expect: the page shows pricing details
